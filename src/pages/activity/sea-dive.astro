---
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/Navbar.astro';
import Footer from '../../components/Footer.astro';
import Contact from '../../components/Contact.astro';
const { title, description, image } = Astro.props;
---

<Layout title="Sea Dive | Gokarna Water Adventures" description="Explore the depths of the Arabian Sea with our thrilling Sea Dive experience. Suitable for beginners and certified divers.">
  <Navbar />
  
  <div class="relative h-[60vh] overflow-hidden">
    <video autoplay loop muted playsinline class="absolute inset-0 w-full h-full object-cover">
        <source src="/videos/gokarna-boat-cruise-service-tadadi.mp4" type="video/mp4">
    </video>
    <div class="absolute inset-0 bg-black bg-opacity-50 flex items-center justify-center relative z-10">
      <div class="text-center text-white">
        <h1 class="text-4xl md:text-6xl font-bold mb-4">Sea Dive</h1>
        <p class="text-xl md:text-2xl mb-8">Dive into the deep blue and discover a new world.</p>
        <a href="#contact" class="bg-blue-600 text-white px-8 py-3 rounded-full text-lg font-semibold hover:bg-blue-700 transition duration-300">Book Now</a>
      </div>
    </div>
  </div>

  <section class="py-16 bg-white">
    <div class="container mx-auto px-4">
      <div class="flex flex-wrap items-center">
        <div class="w-full md:w-1/2 mb-8 md:mb-0">
          <img src="/images/sea-dive.jpg" alt="Diver exploring underwater" class="rounded-lg shadow-xl w-full h-auto object-cover">
        </div>
        <div class="w-full md:w-1/2 md:pl-12">
          <h2 class="text-3xl font-bold mb-6 text-gray-800">Why Choose Sea Diving?</h2>
          <p class="text-gray-600 mb-6 leading-relaxed">
            Sea diving is an unparalleled adventure that allows you to witness the vibrant marine life of Gokarna up close.
            Whether you're a first-timer or an experienced diver, our certified instructors ensure a safe and unforgettable experience.
            Swim alongside colorful fish, explore stunning coral reefs, and experience the weightlessness of the underwater world.
          </p>
          <ul class="list-disc list-inside text-gray-600 mb-6">
            <li>Certified PADI instructors</li>
            <li>State-of-the-art equipment provided</li>
            <li>Suitable for beginners and experts</li>
            <li>Explore vibrant coral reefs and marine life</li>
          </ul>
          <a href="#contact" class="bg-blue-600 text-white px-6 py-2 rounded-full font-semibold hover:bg-blue-700 transition duration-300">Book Your Dive</a>
        </div>
      </div>
    </div>
  </section>

  <section class="py-16 bg-gray-100">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl font-bold mb-12 text-center text-gray-800">What to Expect</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="bg-white p-6 rounded-lg shadow-md text-center">
          <div class="text-blue-600 mb-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-2">Duration</h3>
          <p class="text-gray-600">The entire experience, including training and the dive, lasts approximately 2-3 hours.</p>
        </div>
        <div class="bg-white p-6 rounded-lg shadow-md text-center">
          <div class="text-blue-600 mb-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z" />
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-2">Depth</h3>
          <p class="text-gray-600">Dives typically range from 10 to 30 feet, depending on your comfort level and experience.</p>
        </div>
        <div class="bg-white p-6 rounded-lg shadow-md text-center">
          <div class="text-blue-600 mb-4">
             <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 13a3 3 0 11-6 0 3 3 0 016 0z" />
             </svg>
          </div>
          <h3 class="text-xl font-semibold mb-2">Photography</h3>
          <p class="text-gray-600">Capture your underwater adventure with optional photo and video packages available.</p>
        </div>
      </div>
    </div>
  </section>

  <Contact />
  <Footer />
  
  <a href="tel:9844801435" id="offer-button" class="fixed bottom-4 right-4 bg-gradient-to-r from-yellow-400 to-orange-500 text-gray-900 px-6 py-4 rounded-full shadow-2xl hover:from-yellow-500 hover:to-orange-600 transition duration-300 z-50 flex items-center space-x-4 transform hover:scale-105">
    <div class="flex flex-col">
      <span class="text-xs font-bold uppercase tracking-wider">Limited Period Offer</span>
      <div class="flex items-baseline space-x-2">
        <span class="text-lg line-through text-gray-700">1499/-</span>
        <span id="offer-price" class="text-3xl font-extrabold text-white drop-shadow-sm">799/-*</span>
      </div>
      <span class="text-xs font-medium text-white bg-red-600 px-2 py-0.5 rounded-full mt-1 w-max">Save 47% Today!</span>
    </div>
    <div class="flex flex-col items-center justify-center bg-white bg-opacity-20 p-2 rounded-lg backdrop-blur-sm">
      <span class="text-xs font-semibold text-white">Offer Ends In:</span>
      <span id="timer" class="text-xl font-mono font-bold text-white tabular-nums drop-shadow-sm">20:00</span>
    </div>
    <div class="animate-bounce bg-white p-2 rounded-full">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-orange-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </div>
  </a>

  <script>
    const offerPriceElement = document.getElementById('offer-price');
    const timerDisplay = document.getElementById('timer');
    const duration = 20 * 60 * 1000; // 20 minutes
    const now = new Date().getTime();
    let endTime = localStorage.getItem('offerEndTimeActivity');

    if (!endTime || now > parseInt(endTime)) {
        endTime = now + duration;
        localStorage.setItem('offerEndTimeActivity', endTime);
    }

    function updateTimer() {
        const currentTime = new Date().getTime();
        const distance = parseInt(endTime) - currentTime;

        if (distance < 0) {
            clearInterval(interval);
            if(timerDisplay) timerDisplay.innerHTML = "00:00";
            if(offerPriceElement) offerPriceElement.innerHTML = "999/-*";
        } else {
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);
            if(timerDisplay) timerDisplay.innerHTML = minutes + "m " + seconds + "s ";
        }
    }

    updateTimer();
    const interval = setInterval(updateTimer, 1000);
  </script>
</Layout>